// Перетворіть цей код на TypeScript, вказавши відповідні типи для всіх змінних.

let age: number = 50; // Числовий тип
let name: string = 'Max'; // Рядковий тип
let toggle: boolean = true; // Логічний тип
let empty: null = null; // Null тип
let notInitialize: undefined; // Undefined тип
let callback: (a: number) => number = (a) => { return 100 + a };

export {};
